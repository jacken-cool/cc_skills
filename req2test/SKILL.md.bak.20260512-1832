---
name: buddyskill-auxre-to-test-suite
description: 从 Axure 原型链接或导航、需求文档，一站式产出需求分析文档、飞书思维笔记格式（Markdown 或 .mm 导入格式）测试点设计文档和 CSV 测试用例；融合 testbuddy-skill 的意图识别与工作流调度能力、axure-to-test-suite 的原型读取与飞书格式交付能力。
triggers: []
allowed-tools:
  - select_workflow
  - select_rule
  - load_session
  - add_nodes
  - update_nodes
  - delete_nodes
  - search_nodes
  - rag_search
disable: false
---

# BuddySkill Axure 一条龙测试交付

> 技能中文名：`Axure一条龙测试交付（增强版）`

## 核心定位

本 skill 融合了两种能力：

1. **Axure 原型读取能力**（来自 axure-to-test-suite）：读取 Axure 链接 / 导航，产出需求分析文档
2. **测试设计工作流调度能力**（来自 testbuddy-skill）：意图识别 → 工作流调度 → 生成飞书格式测试点 + CSV 测试用例

支持以下输入来源：
- Axure 原型链接 / 导航
- PDF 产品方案文档
- 已有需求文档（Markdown 格式）
- 对话上下文中的需求描述

## 执行入口：意图识别

根据用户请求识别执行模式：

| 用户意图 | 触发关键词 | 执行路径 |
|---------|-----------|---------|
| Axure 一条龙 | `Axure链接`、`Axure原型`、`axure`、`读取原型` | 进入 Step A（Axure 读取流程） |
| 从需求生成测试框架 | `生成测试框架`、`生成框架`、`创建测试框架` | 进入 Step B（testbuddy 工作流） |
| 从需求生成测试点 | `生成测试点`、`创建测试点`、`生成飞书测试点` | 进入 Step B（feishu-testpoint 工作流） |
| 从需求生成测试用例 | `生成测试用例`、`生成用例`、`生成CSV用例` | 进入 Step B（csv-case 工作流） |
| PDF 一条龙 | `PDF`、`pdf`、`产品方案`、`PRD`、`读取PDF` | 进入 Step A-PDF（PDF 读取流程），可续接 Step B |
| 全链路一次跑完 | `一次跑完`、`一条龙`、`直接出测试文档`、`直接出用例` | 进入 Step A 或 Step A-PDF → 自动续接 Step B |

---

## Step A：Axure 原型读取 → 需求分析

> 当输入来源为 Axure 链接时执行本步骤。

### A1：确认读取范围（读取 Axure 前必须先确认）

按以下顺序处理：

1. 若用户一次给出多个链接或多个导航，按**批量任务**处理。
2. 若用户给出一个链接但包含多导航分支，先确认是读单个目录还是多个批次。
3. 若用户明确说"全部目录"/"全部导航"/"整条链路"，按全量读取处理。
4. 若用户给出单个目录或单个页面，按单目录处理。
5. 若**未说明范围**，先让用户选择：
   - 读取单个目录 / 单个页面
   - 读取全部目录 / 全部导航

批量任务要求：
- 给每个链接 / 导航分支编号。
- 明确每一项的读取范围和输出方式（独立 / 汇总）。

**超时兜底**（适用于"未说明范围需用户选择"的场景）：
- 3 分钟无响应 → 发送提示「请选择读取范围：①单个目录/页面 ②全部目录/导航。回复序号即可」
- 5 分钟仍无响应 → **默认按"单个目录/页面"处理** → 读取第一页/默认导航 → 标记「⚠️ 范围待确认，如需调整请告知」

### A2：读取 Axure，产出需求分析文档

#### A2a：读取方法优先级

按以下优先级尝试读取 Axure 原型：

**方案一（首选）：curl 直接抓取 Axure 源文件**
适用于任何可通过 HTTP(S) 访问的 Axure 链接，尤其适合内网环境：
1. 根据用户提供的 URL 定位 Axure 发布目录
2. 依次抓取以下文件：
   - `data/document.js` — 提取页面清单（页面 ID、名称、URL），发现所有子页面
   - 每个子页面的独立 HTML 文件（格式：`_{pageName}.html`）— 提取页面内的完整文案
   - `start.html` 或 `index.html` — 主框架（如需要）
3. 从每个页面的 HTML 中提取 `div#base` 下的 `div_text` 内容，获取原型上所有可见文案
4. 若存在表格、流程图等结构化内容，从页面 HTML 的 div 结构中还原业务规则

**方案二（备选）：agent-browser 浏览器自动化**
若 Axure 渲染后内容与源文件差异较大（如动态交互效果），可使用浏览器渲染后读取。

**方案三（兜底）：WebFetch**
外部可访问的 Axure 链接可使用 WebFetch 读取内容。

执行要求：

1. 优先读取导航、正文、iframe 内容与关键页面文案。
2. 若为多页导航，先通过 `data/document.js` 发现所有页面，再逐页抓取整理。
3. 若需截图，补充截图文件。
4. 若为批量任务，每个链接 / 导航分组 = 一个分析单元，分别整理。

输出 Markdown 需求分析文档，至少包含：
- 文档目的
- 导航结构与覆盖范围
- 总体需求结论
- 分页需求分析
- 跨页面业务链路
- 已确认规则
- 风险点与待确认项

推荐命名：
- 单页：`axure_requirement_analysis.md`
- 多页整合：`axure_navigation_requirement_analysis.md`
- 批量场景：`01_xxx_requirement_analysis.md`、`02_xxx_requirement_analysis.md`

### ⛔ 确认门 A3：需求分析确认与继续方式

> 从"AI 自动判断"改为"AI 给出建议 + 用户主动确认"。

#### A3a：输出分析摘要

完成需求分析后，向用户展示以下摘要（控制在 5 条以内）：

1. **覆盖范围**：共分析 X 个页面 / 模块，涉及 Y 个核心功能点
2. **关键发现**：需求规则是否清晰、有无字段冲突、业务链路是否完整
3. **风险点**：列出 1-3 个可能影响测试设计的问题
4. **建议**：基于以下判断标准给出「建议继续」或「建议先确认」

#### A3b：判断标准（供 AI 生成建议时参考）

**建议停下确认的情况**：
1. 用户明确要求"先出需求分析我确认后再继续"。
2. 原型存在关键规则不清、字段冲突、业务链路缺失。
3. 批量任务中各链接 / 各导航差异较大，需确认是否合并口径。
4. 读取过程中出现范围变化（单目录变多目录等）。
5. 测试点设计依赖用户尚未确认的关键策略（搜索规则、权限规则等）。

**建议直接继续的情况**：
1. 用户明确要求"一次跑完"/"直接出完整交付物"。
2. 需求规则、字段、流程已足够清晰。
3. 待确认项不阻塞核心测试点设计。
4. 批量任务结构相近，可按统一模板输出。

#### A3c：用户确认与执行

**话术模板**：
```
【需求分析摘要】
- 覆盖范围：...
- 关键发现：...
- 风险点：...

【我的建议】
✅ 建议直接继续生成测试文档（原因：...）
或
⚠️ 建议先确认以下事项再继续（原因：...）

请确认：
① 直接继续生成测试点 → 回复「继续」
② 先确认需求分析 → 回复「确认需求分析」，我会列出待确认项
```

**用户确认后的执行路径**：
- 用户回复「继续」→ 基于需求分析产出**测试点初稿** → 进入确认门 A4
- 用户回复「确认需求分析」→ 交付完整需求分析文档 → 列出待确认项 → 等用户确认后继续

#### 超时兜底

- 3 分钟无响应 → 发送提示「请确认是否继续生成测试文档？回复『继续』或『确认需求分析』」
- 5 分钟仍无响应 → **默认按「继续」处理**：产出测试点初稿 → 进入确认门 A4 → 标记「⚠️ 继续方式待确认」

---

### ⛔ 确认门 A4：测试点数量与格式确认

**触发时机**：完成测试点初稿后，进入最终输出前

**前置状态**：需求分析已完成，且用户已在确认门 A3 确认「继续」，已产出测试点初稿。

**确认内容**：
1. **测试点数量**：预计生成 X 条测试点，是否符合预期？
2. **输出格式**：
   - 飞书思维笔记格式（.mm 文件，支持导入飞书文档）
   - Markdown 格式（可复制到飞书）
   - CSV 格式（导入 TestLink / Zephyr 等用例管理工具）
3. **分层级别**：是否需要按「P0/P1/P2」分级？

**超时兜底**：
- 3 分钟无响应 → 发送提示「请确认上方格式选择，回复序号即可」
- 5 分钟仍无响应 → 默认输出「飞书思维笔记 + Markdown」双版本，标记「⚠️ 格式待确认」

---

## Step A-PDF：PDF 产品方案文档 → 需求分析

> 当输入来源为 PDF 文件时执行本步骤。

### A-PDF1：PDF 文本提取

#### 提取方法优先级

| 优先级 | 方法 | 适用场景 |
|:---:|---|---|
| 1（首选） | Python pymupdf（fitz）库 | 中文 PDF、文本型 PDF、图文混合 PDF |
| 2（备选） | pdftotext 命令行 | 纯英文 PDF，或 pymupdf 安装失败时的回退 |
| 3（兜底） | Read 工具直接读取 | 仅当 PDF 页数 ≤ 3 且内容简短时尝试 |

#### pymupdf 提取流程

1. 检查 pymupdf 是否已安装：`python -c "import fitz"`
2. 若未安装：`pip install pymupdf`
3. 执行提取（使用 `scripts/extract_pdf.py` 或等效内联代码）：

```python
import fitz, sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
doc = fitz.open('PDF文件路径')
for i, page in enumerate(doc):
    text = page.get_text()
    print(f'\n===== 第{i+1}页 =====')
    print(text)
```

4. 若提取结果为空或仅含碎片字符 → 判断 PDF 为扫描件/图片型 → 标注「⚠️ 无法提取文本，需手动提供需求内容」

#### pymupdf 安装失败时的回退

若 `pip install pymupdf` 失败（如离线环境）：
- 检查是否有 `pdftotext`：`which pdftotext`
- 若有 → 执行 `pdftotext "文件路径" -`（无 layout 参数，保留原始换行）
- 若结果中文乱码 → 标注「⚠️ PDF 中文提取受限，部分内容可能缺失」
- 若 `pdftotext` 也不可用 → 使用 Read 工具直接读取 PDF，标注「⚠️ PDF 内容可能不完整」

### A-PDF2：需求信息结构化

从 PDF 提取的原始文本中，按以下框架组织为需求分析：

| 板块 | 提取来源 | 组织方式 |
|------|---------|---------|
| 需求概述 | 标题 + 核心目标一句话总结 | 从文档开头提取 |
| 需求背景 | "需求背景"/"背景" 章节 | 场景 + 痛点 + 期望，三点式 |
| 功能说明 | "功能说明"/"方案说明" 章节 | 处理逻辑表格 + 参数映射表 + 规则对照表 |
| 已确认规则 | 从功能说明中提炼 | R1、R2...编号，每条对应一条可验证的业务规则 |
| 残留风险点 | 文档中未明确/空白/标注异常的内容 | ?1、?2...编号，含影响范围和建议 |
| 涉及端点 | 从功能描述中反推 | 前端页面 + 后端接口 + 数据表 |
| 参数汇总 | 从参数定义和对照表中整合 | 参数在各阶段的出现情况矩阵 |

### A-PDF3：输出需求分析文档

输出格式要求与 Step A2 一致（详见 Step A2 末尾的输出规范），至少包含：
- 需求概述
- 需求背景
- 功能模块（含处理逻辑、参数映射、规则对照表）
- 已确认的规则摘要
- 残留风险点 / 待确认项
- 涉及端点与页面
- 参数汇总

命名：`axure_requirement_analysis.md`（与 Axure 路径保持一致，便于 Step B 统一读取）。

### 确认门复用

PDF 路径完成需求分析后，复用确认门 A3 和 A4（与 Axure 路径共用同一确认流程），即：
- A3：输出分析摘要 → 用户确认是否继续
- A4：测试点数量与格式确认

---

## Step B：需求文档 → 飞书测试点 + CSV 测试用例

> 当输入来源为已有需求文档、或 Step A / Step A-PDF 完成后继续时执行本步骤。
> 本步骤为意图识别调度器，根据用户意图选择对应工作流。

### 异常处理机制

| 异常场景 | 处理策略 |
|---------|---------|
| Axure 链接无效/无法访问 | 提示用户检查链接，询问是否改用截图或粘贴原型描述 |
| agent-browser 启动失败（Chrome 沙箱/路径问题） | 降级使用方案一：curl 直接抓取 Axure 源文件（`data/document.js` + 各页面 HTML） |
| WebFetch 无法验证内网域名 | 降级使用 curl 直接抓取 Axure 源文件，绕过外部网络验证限制 |
| Axure 页面内容在 JS 渲染后呈现（直接抓取为空白） | 尝试读取 `data/document.js` 获取数据，或截图备用 |
| 工作流文件加载失败 | 回退到内置简化流程，标注「⚠️ 使用降级模式」 |
| 批量任务中途失败 | 记录已完成部分，询问用户是否继续剩余项 |
| 格式转换失败（.mm 生成错误） | 自动回退为 Markdown 格式，标注需手动导入飞书 |
| 工具调用超时 | 重试 1 次，失败则使用手动模式继续 |
| PDF 文件无法读取（加密/损坏） | 提示用户检查文件，询问是否提供截图或粘贴文档内容 |
| PDF 为扫描件/图片型，pymupdf 提取为空 | 标注「⚠️ 无法自动提取文本」，请用户手动提供需求描述，或逐一截图 |
| pymupdf 安装失败且无 pdftotext | 使用 Read 工具尝试直接读取，标注「⚠️ PDF 内容可能不完整」 |

---

### B1：识别生成意图

| 用户意图 | 意图关键词 | 对应工作流 |
|---------|-----------|-----------|
| 生成测试框架 | `框架生成` | `workflows/framework-generation.md`（testbuddy 原生） |
| 生成测试模块 | `模块生成` | `workflows/feature-generation.md`（testbuddy 原生） |
| 生成飞书格式测试点 | `测试点生成` | `workflows/feishu-testpoint-generation.md`（本 skill 增强） |
| 生成 CSV 测试用例 | `用例生成` | `workflows/csv-case-generation.md`（本 skill 增强） |

### B2：加载会话参数

#### 需加载的会话参数清单

| 参数名 | 说明 | 来源 | 是否必填 |
|--------|------|------|:--------:|
| `requirement_doc` | 需求分析文档路径 / 内容 | Step A2 产出或用户直接提供 | ✅ |
| `scope_confirmed` | 已确认的读取范围（单页/多页/批量） | Step A1 确认结果 | ✅ |
| `user_intent` | 用户生成意图（测试框架/测试点/CSV用例） | Step B1 识别结果 | ✅ |
| `output_format` | 用户偏好的输出格式（.mm / Markdown / CSV） | 确认门 A4 结果 | ⚠️ 缺省时按 A4 兜底策略处理 |
| `priority_level` | 是否需要 P0/P1/P2 分级 | 确认门 A4 结果 | ❌ |
| `history_deliverables` | 历史交付物路径（增量任务时需要） | 用户指定或会话上下文 | ❌ |

#### 参数缺失处理策略

| 缺失场景 | 处理策略 |
|---------|---------|
| `requirement_doc` 缺失 | 检查当前工作空间是否有 `axure_requirement_analysis.md` 或类似文件；若无，提示用户补充需求文档或返回 Step A2 重新生成 |
| `scope_confirmed` 缺失 | 默认按「单个目录/页面」处理，标记「⚠️ 范围待确认」 |
| `user_intent` 缺失 | 基于需求文档内容自动推断最可能的意图（内容侧重规则/流程 → 测试点；内容侧重字段校验 → CSV用例） |
| `output_format` 缺失 | 默认输出「飞书思维笔记 + Markdown」双版本 |

#### 失败回退

- `load_session` 工具调用失败 → 从当前对话上下文中手动提取上述参数 → 若仍无法获取关键参数（`requirement_doc` / `user_intent`），暂停并询问用户
- 参数提取不完整 → 按缺失处理策略执行，标注「⚠️ 部分参数来自推断」

### B3：选择并调用工作流

1. 使用 `tools/select_workflow.md` 工具，传入步骤 B1 识别的意图关键词。
2. 根据返回路径类型选择读取方式：
   - 路径以 `.codebuddy/rules/` 开头（自定义工作流）：使用 `read_rules` 读取
   - 路径以 `workflows/` 开头（默认工作流）：使用 `read_file` 读取
3. 交由工作流执行具体逻辑。
4. **正常情况下**须通过 select_workflow 选择后再读取，禁止绕过选择器直接读取。

#### 失败回退路径

| 失败场景 | 回退策略 |
|---------|---------|
| `select_workflow` 工具不可用/超时 | 根据 B1 的意图关键词手动映射到工作流路径 → 使用 `read_file` 直接读取 |
| `read_rules` / `read_file` 读取工作流失败 | 使用内置简化逻辑：从需求文档直接提取测试点/用例，按「输出质量规则」生成标准格式，标注「⚠️ 工作流读取失败，使用内置简化逻辑」 |
| 工作流文件内容异常（损坏/为空） | 同上，使用内置简化逻辑，并提示用户工作流文件需修复 |

**内置简化逻辑要点**：
- 测试点：按飞书思维笔记层级格式输出（模块 → 场景 → 测试点）
- 测试用例：按标准 CSV 字段输出（用例编号、模块、标题、前置条件、操作步骤、预期结果、优先级）
- 优先级默认按业务重要性分配 P0/P1/P2

---

## 全链路追溯要求

确保 Axure 页面 → 需求分析 → 测试点 → 测试用例四层可追溯：

1. 需求分析中标出页面来源与链路位置。
2. 测试点中对应需求模块与关键规则。
3. 测试用例中填写关联需求字段。
4. 无法从原型直接确认的内容统一标注"待确认"。
5. 若用户要求保留历史结果，做增量整合而不是覆盖。
6. 批量任务中输出总览清单，标明每项来源与交付物对应关系。

---

## 工作流能力矩阵

| 生成目标 | 触发关键词 | 输入 | 输出 |
|---------|-----------|------|------|
| 测试框架 | `framework`/`框架` | STORY/BUG | FEATURE/SCENE/TEST_POINT |
| 测试模块 | `feature`/`模块` | STORY/BUG | FEATURE |
| 飞书测试点（Markdown） | `测试点`/`feishu` | FEATURE/SCENE/需求文档 | 飞书层级 Markdown |
| 飞书测试点（.mm） | `mm`/`.mm`/`导入飞书` | FEATURE/SCENE/需求文档 | .mm 思维导图文件 |
| CSV 测试用例 | `case`/`用例`/`csv` | STORY/BUG/FEATURE/TEST_POINT | CSV 测试用例 |

---

## 工具能力矩阵

| 工具 | 功能 | 文档路径 |
|-----|------|---------|
| select_workflow | 智能选择工作流 | `tools/select_workflow.md` |
| select_rule | 智能选择规则 | `tools/select_rule.md` |
| load_session | 加载会话参数 | `tools/load_session.md` |
| add_nodes | 添加节点到脑图 | `tools/add_nodes.md` |
| update_nodes | 修改脑图节点 | `tools/update_nodes.md` |
| delete_nodes | 删除脑图节点 | `tools/delete_nodes.md` |
| search_nodes | 查询脑图节点 | `tools/search_nodes.md` |
| rag_search | 知识库检索 | `tools/rag_search.md` |

---

## 输出质量规则

- 一次任务内保持"先需求分析 → 判断是否暂停 → 飞书测试点 → CSV 用例"的顺序。
- 范围不明确时先问范围，再读取 Axure。
- 默认自动判断，遇到关键待确认项优先停下确认。
- 测试点强调覆盖结构（飞书思维笔记可直接导入）。
- 测试用例强调执行动作与预期结果（CSV 可直接导入测试平台）。
- `步骤描述` 与 `预期结果` 默认使用 `1.` `2.` `3.` 多行分点格式。
- **CSV 单元格内换行必须使用真正的换行符**（双引号包裹的字段内直接换行），不能用 `\n` 字面文本。
- **CSV 字段值中避免出现英文逗号 `,`**（会破坏列结构），改用中文逗号 `，` 或调整语序。
- 多系统联动必须串出完整链路。
- 批量任务保持命名、编号、模块口径一致。
- 若用户已给过模板、命名或字段顺序，以用户要求为准。

---

## Resources

### references/axure_reading_workflow.md

Axure 原型读取详细执行路径、自动停顿判断规则、批量处理方式、典型提示词与注意事项。

### references/mm_mindnote_format.md

飞书思维笔记 `.mm` 格式规范，包括：
- XML 文件结构
- ID 生成规则（MD5 哈希）
- STYLE 属性（bubble / fork）
- POSITION 属性（left / right）
- Markdown 与 .mm 格式转换规则

### assets/deliverables_template.md

整套交付物清单模板，在最终汇总时明确需求分析、测试点、测试用例三类产物。

### assets/test_points_feishu_template.mm

飞书思维笔记可直接导入的 `.mm` 格式模板，包含测试点标准模块结构。

### workflows/feishu-testpoint-generation.md

飞书格式测试点生成工作流：从需求文档或脑图节点，生成可导入飞书思维笔记的 Markdown 层级或 .mm 文件。

### workflows/csv-case-generation.md

CSV 测试用例生成工作流：从测试点或需求文档，生成标准字段格式的 CSV 测试用例文件。

### scripts/extract_pdf.py

可复用的 PDF 文本提取脚本，封装 pymupdf：接收 PDF 路径 → 输出纯文本。同时可用于手动调试。
